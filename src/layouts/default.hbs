<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>{{title}}</title>

		{{# styles }}
		<link rel="stylesheet" href="./css/{{ . }}.css">
		{{/ styles }}

	</head>
	<body>
		{{> header }}

		{{> body }}

		{{> footer }}

		{{# scripts }}
		<script src="./js/{{ . }}.js"></script>
		{{/ scripts }}
	</body>
</html>
